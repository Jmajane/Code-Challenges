// take array and push into a string
// create for loop to go through the array



// let nums = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0]

// function createPhoneNumber (arr) {
//     let first = arr.splice(0,3)
//     let second = arr.splice(0,3)
//     let third = arr.splice(0,4)
//     let areaCode = (`(${first.join('')})-${second.join('')}-${third.join('')}`)
//     console.log(areaCode)
// }

// createPhoneNumber(nums)